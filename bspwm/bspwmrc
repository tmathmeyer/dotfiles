#! /bin/sh

PATH="$HOME/.xbin/:$PATH"

if [ -e ~/.config/bspwm/daemons.sh ]; then
  exec ~/.config/bspwm/daemons.sh
fi

if [ -e ~/.config/bspwm/layout.sh ]; then
  exec ~/.config/bspwm/layout.sh
fi

if [ -e ~/.config/bspwm/wallpaper.sh ]; then
  exec ~/.config/bspwm/wallpaper.sh
fi
